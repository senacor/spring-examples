This project deals with the servlet filter handling in Spring MVC.
It gives an overview about servlet filter definition and registration with Spring.
Moreover, error handling with ans custom error controller is introduced.

To run the project, execute mvn spring-boot:run
